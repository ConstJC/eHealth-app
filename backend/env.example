# Database
DATABASE_URL="postgresql://jcode:password@localhost:5000/ehealth_db?schema=public"

# Stage
NODE_ENV=development

# API Server Port (default: 4081)
PORT=4081
# Backend API URL (default: http://localhost:4081)
BACKEND_URL=http://localhost:4081
# Frontend URL (default: http://localhost:8000)
FRONTEND_URL=http://localhost:8000

# ===== For Docker Compose =====
DB_USER=jcode
DB_PASSWORD=password
DB_NAME=ehealth_db
DB_PORT=5000
REDIS_PORT=6666
PGADMIN_PORT=5050
PGADMIN_EMAIL=jcode@pgadmin.com
PGADMIN_PASSWORD=password
# ===== For Docker Compose =====

# JWT Secrets
JWT_ACCESS_SECRET==secret-key
JWT_REFRESH_SECRET=refresh-key
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=your-email@gmail.com

# Rate Limiting
THROTTLE_TTL=60
THROTTLE_LIMIT=10
