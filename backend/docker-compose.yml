name: nestjs-auth-api

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '${FRONTEND_URL}:3000'
    environment:
      - STAGE=${STAGE}
      - DB_URL=${DB_URL}
      - TEST_DB_URL=${TEST_DB_URL}