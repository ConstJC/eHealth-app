# Frontend Port (Next.js defaults to 3000 if not set)
# PORT=3000

# Backend API URL (with version prefix)
NEXT_PUBLIC_API_URL=http://localhost:4081/api/v1

# App URL (for email verification links, etc.)
NEXT_PUBLIC_APP_URL=http://localhost:3000

